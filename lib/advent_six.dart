int distance(String coords) {
  List<int> data = parse(coords);
}

//get extents.....
//squares around each distance if two or more same distance change to .
//int for distance, and String for char
//build sum that finds the largest char that is within the square.

List<int> parse(String coords) {

}